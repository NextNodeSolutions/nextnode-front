---
import { cn } from '@/lib/utils'
import { languages } from '@/lib/constants'
import { Sun, ChevronDown } from 'lucide-react'

interface Props {
	variant?: 'desktop' | 'mobile'
}

const { variant = 'desktop' } = Astro.props
---

<div class={cn(
  variant === 'desktop' ? 'flex items-center space-x-6' : 'pt-4 border-t border-gray-200'
)}>
  <div class={cn(
    variant === 'desktop' ? 'flex items-center space-x-6' : 'flex items-center justify-between'
  )}>
    <!-- Theme Toggle -->
    <button
      class="p-1.5 rounded-full text-gray-600 hover:text-gray-900 hover:bg-gray-100 transition-all duration-300 hover:scale-110"
      aria-label="Toggle theme"
    >
      <Sun className="w-4 h-4" />
    </button>

    <!-- Language Selector -->
    <div class="relative group">
      <button
        class="flex items-center space-x-1.5 text-gray-600 hover:text-gray-900 transition-all duration-300 hover:scale-105"
        aria-label="Select language"
      >
        <span class="text-base">🇬🇧</span>
        <span class="text-xs">EN</span>
        <ChevronDown className="w-3 h-3 transition-transform duration-300 group-hover:rotate-180" />
      </button>
      <div class="absolute right-0 mt-2 w-40 bg-white/95 backdrop-blur-sm border border-gray-200 rounded-lg shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 transform group-hover:translate-y-0 translate-y-2">
        {languages.map((lang) => (
          <a
            href={`?lang=${lang.code}`}
            class="flex items-center space-x-2 px-4 py-2 text-gray-600 hover:text-gray-900 hover:bg-gray-50 transition-all duration-300 hover:translate-x-1"
          >
            <span class="text-lg">{lang.flag}</span>
            <span>{lang.label}</span>
          </a>
        ))}
      </div>
    </div>
  </div>
</div> 