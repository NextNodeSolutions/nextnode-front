---
import DesktopControls from './controls-desktop.astro'
import MobileControls from './controls-mobile.astro'

interface Props {
	variant?: 'desktop' | 'mobile'
}

const { variant = 'desktop' } = Astro.props
---

{variant === 'desktop' ? <DesktopControls /> : <MobileControls />}
