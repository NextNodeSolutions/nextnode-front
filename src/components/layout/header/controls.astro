---
import DesktopControls from './desktop/controls-desktop.astro'
import MobileControls from './mobile/controls-mobile.astro'

interface Props {
	variant?: 'desktop' | 'mobile'
}

const { variant = 'desktop' } = Astro.props
---

{variant === 'desktop' ? <DesktopControls /> : <MobileControls />}
