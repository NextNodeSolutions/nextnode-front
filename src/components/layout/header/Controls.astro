---
import DesktopControls from './desktop/ControlsDesktop.astro'
import MobileControls from './mobile/ControlsMobile.astro'

interface Props {
	variant?: 'desktop' | 'mobile'
}

const { variant = 'desktop' } = Astro.props
---

{variant === 'desktop' ? <DesktopControls /> : <MobileControls />}
