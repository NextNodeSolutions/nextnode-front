---
import StepCard from './StepCard.astro'
import { cn } from '@/lib/core/utils'

interface Props {
	stepKey: string
	index: number
	className?: string
	variant?: 'mobile' | 'compact' | 'mini'
}

const { stepKey, index, className = '', variant = 'mobile' } = Astro.props
---

<div
	class={cn(
		'animate-in slide-in-from-bottom-8 fade-in-0 duration-700 fill-mode-forwards opacity-0 motion-reduce:animate-none motion-reduce:opacity-100 hover:translate-x-1 transform transition-all',
		className,
	)}
	style={`animation-delay: ${index * 150}ms`}
	data-step-card
	data-step-index={index}
>
	<StepCard stepKey={stepKey} index={index} variant={variant} />
</div>
