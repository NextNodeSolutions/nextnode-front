name: Tests

on:
    pull_request:
        branches: [main, master]

jobs:
    quality-checks:
        name: Quality Checks
        uses: NextNodeSolutions/github-actions/.github/workflows/quality-checks.yml@main
        with:
            run-lint: true
            run-typecheck: true
            run-tests: true
            run-build: false
            run-security: false
            test-coverage: false
