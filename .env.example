# Email Configuration
# Only the API key is needed as environment variable for security
# Email addresses and other settings are now in config JSON files
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Google Analytics Configuration
# Optional - if not provided, GA tracking will be disabled
GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Environment (optional - defaults to 'development')
NODE_ENV=development